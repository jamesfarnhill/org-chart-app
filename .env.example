# Supabase — copy to .env and fill in from your project's API settings.
# Safe to expose in the browser (these are the public/anon values):
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# If these are blank, the app falls back to the local-only demo accounts.
